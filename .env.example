# Copy to `.env` and fill in values (`.env` and `.env.local` are gitignored).
# Optional: `.env.local` for machine-specific overrides (loaded after `.env`).

DATABASE_URL=sqlite:///./nudge.db

CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

OPENAI_API_KEY=

PORT=8000
